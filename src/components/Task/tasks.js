// export const Tasks = ({ tasks, loading }) => {
//   if (loading) {
//     return <h2>Loading...</h2>;
//   }

//   return (
//     <ul className="mb-4">
//       {tasks.map((task) => (
//         <li key={task.id} className="list-group-item">
//           {task.title}
//         </li>
//       ))}
//     </ul>
//   );
// };
